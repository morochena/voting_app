<!DOCTYPE html>
<html>
  <head>
    <title> <%= full_title(yield(:title)) %> </title>
    <%= render "rails_default" %>
  </head>

  <body>
  <%= render "header" %>

    <div class="container" >
    <%= yield %>
    </div>
  <%= render "footer" %>
  </body>
</html>
